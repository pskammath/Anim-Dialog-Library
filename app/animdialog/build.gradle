apply plugin: 'maven-publish'
// Apply this plugin at the top of your library build.gradle


def getVersionName = { ->
    return "1.0.2" // Replace with version Name
}

def getArtificatId = { ->
    return "animdialog" // Replace with library name ID
}


afterEvaluate {
    publishing {
        publications {
            bar(MavenPublication) {

                groupId = 'com.psoft' // Replace with group ID
                artifactId = getArtificatId()
                version = getVersionName()

            }
        }

    }
}
